from services.schema import ProfileSchema


def login_social_user(profile: ProfileSchema):
    # TODO endpoint to log a user in
    return ""

def create_social_user(profile: ProfileSchema):
    # TODO: endpoint to create a user, it may receive other data
    return ""